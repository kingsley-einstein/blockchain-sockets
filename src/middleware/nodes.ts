// import express from "express";
// import { P2P } from "../p2p";

// export const nodes = (p2p: P2P) => {
//  return async (req: express.Request & { nodes: any }, res: express.Response, next: express.NextFunction) => {
//   req.nodes = (await p2p.getNodes());
//   next();
//  }
// }
