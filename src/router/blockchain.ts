// import { Router } from "express";
// import { ChainController } from "../controllers";

// const router = Router();

// router.post("/add/:txId/:address", ChainController.addToChain);

// export default router;
